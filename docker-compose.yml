services:
  app:
    build: .
    container_name: laravel-microservice
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - .:/var/www/html
      - /var/www/html/vendor
    command: php artisan serve --host=0.0.0.0 --port=8000
